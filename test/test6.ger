start
    import 'newpackage.ger';

    struct Vehicle {
        type string;
        regNumber int;
        chassieNumber string;
        color string;
        
        getManufacturerName function string(){
            print manufacturer_name;
        }
        
        owner string;
    }

    struct Person {
        id string;
        name string;
        age number;
    }

    if(a == 10){
        let string name = 'naveen dhananjaya';
    }else{
        print 'naveen hettiwaththa!';
    }

    let ArrayList arr = [1,2,3 , [4,5,6] , myFunc([1,2,3])];
    myFunc(arr[1 + 2]);

end